config:
  send_anonymous_usage_stats: False

default:
  outputs:

    dev: # User-Password config
      type: snowflake
      account: ls72422.ap-south-1.aws
      user: DBT_CLOUD_DEV
      password: abc123
      role: TRANSFORMER_DEV
      database: DEV
      warehouse: TRANSFORMING
      schema: dbt_nuthan
      threads: 2
      client_session_keep_alive: False

  target: dev
